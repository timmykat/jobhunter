= form_for @recruiter do |f|
  - if @recruiter.errors.any?
    #error_explanation
      %h2= "#{pluralize(@recruiter.errors.count, "error")} prohibited this recruiter from being saved:"
      %ul
        - @recruiter.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :company
    = f.text_field :company
  .actions
    = f.submit 'Save'
